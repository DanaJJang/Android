<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.danajjang">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Danajjang"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.ChoiceActivity"></activity>
        <activity android:name=".activity.MeaningTestActivity" />
        <activity android:name=".activity.EnglishTestActivity" />
        <activity android:name=".activity.SignWordActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AddWordPadActivity" />
        <activity android:name=".activity.SignUpActivity" />
        <activity android:name=".activity.ListActivity" />
    </application>

</manifest>